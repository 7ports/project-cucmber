**05:42** ✓ `scrum-master` [task_complete] Pierce fix implemented on two fronts. Code: projectileBehaviour.cs now de-dupes hits per distinct enemy (HashSet on attachedRigidbody, reset in OnEnable) so a multi-collider enemy counts as one hit. Editor: created EnemyBody(10) and PlayerProjectile(11) layers, moved each enemy's solid body collider to an EnemyBody child (via slime base prefab, all variants inherit), moved star projectile to PlayerProjectile, and set the Physics2D matrix so enemy bodies no longer collide with the player or player projectiles (contact-damage trigger + wall collisions preserved). Editor reports no compile errors. Remaining: user Play-Mode confirmation, then commit + push.
